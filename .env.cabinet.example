# ============================================
# Configuration Mode CABINET (Cabinet Comptable)
# Base de données dédiée au cabinet
# Gère plusieurs dossiers clients dans la même DB
# ============================================

# Version de l'application
VERSION=latest

# Identifiant Unique du Cabinet
# IMPORTANT: Doit être unique (ex: cabinet-douala, expertise-comptable-yaounde, etc.)
CABINET_ID=cabinet-douala

# Nom du cabinet
CABINET_NAME=Cabinet Expert Comptable Douala

# Domaine dédié au cabinet
CABINET_DOMAIN=cabinet-douala.predykt.com

# Ports uniques pour ce cabinet
# ATTENTION: Chaque cabinet doit avoir des ports différents
APP_PORT=8100
DB_PORT=5500
REDIS_PORT=6500
PGADMIN_PORT=5150

# Subnet unique (pour éviter les conflits réseau Docker)
# Valeur entre 20-254 (chaque cabinet doit être différent)
CABINET_SUBNET_OCTET=30

# Base de données dédiée au cabinet
DB_NAME=predykt_cabinet_${CABINET_ID}
DB_USER=predykt_cabinet_${CABINET_ID}
DB_PASSWORD=CHANGEME_DB_PASSWORD_CABINET

# Redis dédié au cabinet
REDIS_PASSWORD=CHANGEME_REDIS_PASSWORD_CABINET

# JWT (clé unique par cabinet)
# IMPORTANT: Générer une clé de 512 bits minimum
# Commande: openssl rand -base64 64 | tr -d "=+/" | cut -c1-64
JWT_SECRET=CHANGEME_JWT_SECRET_MINIMUM_64_CHARACTERS_REQUIRED_FOR_HS512_ALGORITHM
JWT_EXPIRATION=86400000

# PgAdmin
PGADMIN_EMAIL=admin@${CABINET_DOMAIN}
PGADMIN_PASSWORD=CHANGEME_PGADMIN_PASSWORD

# Environnement
SPRING_PROFILES_ACTIVE=cabinet
PREDYKT_TENANT_MODE=CABINET
PREDYKT_TENANT_CABINET_ID=${CABINET_ID}

# Informations additionnelles (optionnel)
# CABINET_CONTACT_EMAIL=contact@cabinet-douala.com
# CABINET_CONTACT_PHONE=+237690000000
# CABINET_ADDRESS=123 Avenue de la Réunification, Douala

# Limites par cabinet (optionnel)
# MAX_COMPANIES=50
# MAX_USERS=20
# MAX_STORAGE_GB=100

# ============================================
# INSTRUCTIONS:
# 1. Copier et renommer: cp .env.cabinet.example .env.cabinet-douala
# 2. Remplacer CABINET_ID et tous les CHANGEME
# 3. Vérifier que les ports sont uniques (pas de conflit)
# 4. Lancer: docker-compose -f docker-compose.cabinet.yml --env-file .env.cabinet-douala up -d
#
# NOTES:
# - Un cabinet = une instance dédiée
# - Chaque dossier client = une company_id dans la DB du cabinet
# - Isolation au niveau ligne (comme SHARED) mais DB séparée
# ============================================
